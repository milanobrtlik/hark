# Copy to `.env` and fill in a free AcoustID application API key to enable the
# acoustic-fingerprint cover/metadata fallback. Register one at:
#   https://acoustid.org/new-application
# Without a key, hark builds fine and the fallback is simply disabled.
ACOUSTID_API_KEY=
